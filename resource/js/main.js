class Projector{
    constructor(){
        this.initialize();
    }
    initialize(){
       console.log("Projector Starting");
        let width=$(selector).width();
        let height=$(selector).height();
    }
}