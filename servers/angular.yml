angular:
  build:
    context: ./build
    dockerfile: Dockerfile.angular
  container_name: angular
  command: "sh run.sh"
    
  volumes:
    - ./nginx/web/timt:/app
  environment:
    - API_URL=${API_URL}
    - MODE=${MODE}
  working_dir: /app