webssh:
  image: webssh-server:latest
  restart: always
  build:
    context: ./build
    dockerfile: Dockerfile.wssh
  container_name: wssh
  command: "wssh --address=0.0.0.0 --port=8080"
  environment:
    - WSSH_DEBUG=true
  network_mode: "host"