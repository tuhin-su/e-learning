nginx:
  image: nginx:latest
  container_name: nginx
  restart: always
  ports:
    - ${SERVER_PORT}:80
  volumes:
    - ./nginx/conf.d:/etc/nginx/conf.d
    - ./nginx/web/timt/dist/timt:/usr/share/nginx/html
    - ./nginx/web/download:/usr/share/nginx/download